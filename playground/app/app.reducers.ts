import {
  ActionReducerMap
} from '@ngrx/store'

import { State } from '@authumn/angular'

export interface State {
  // auth: fromAuthumn.State
}

export const reducers: ActionReducerMap<State> = {
  // auth: fromAuthumn.reducers
}
